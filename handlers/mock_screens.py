from aiogram import Router, types
from aiogram.filters import Command
from config import main_kb # Импортируй клавиатуру, чтобы было как в жизни

router = Router()
ADMIN_IDS = [] # TODO: Replace with actual admin Telegram IDs

@router.message(Command("mock_summary"))
async def mock_summary(message: types.Message):
    if message.from_user.id not in ADMIN_IDS: return

    # ИДЕАЛЬНЫЙ ТЕКСТ (без слова "Тест", красивый Markdown)
    text = (
        "📅 *Результат обработки запроса*\n\n" # TODO: Customize text
        "_Сгенерированный анализ данных. Этот текст демонстрирует, как должен выглядеть "
        "финальный результат работы AI-модели, включая форматирование и структуру. "
        "Замените этот текст на реальный вывод вашего AI-промпта._\n\n"
        "*Пример обработанных данных:*\n"
        "✅ Элемент данных 1\n"
        "✅ Элемент данных 2\n"
        "✅ Элемент данных 3\n"
        "✅ Элемент данных 4\n"
        "✅ Элемент данных 5\n\n"
        "--- Конец отчета ---"
    )
    await message.answer(text, parse_mode="Markdown")

@router.message(Command("mock_pirate"))
async def mock_pirate(message: types.Message):
    if message.from_user.id not in ADMIN_IDS: return

    # Имитация диалога (ты сам напишешь вопрос, а это ответ бота)
    text = (
        "Ответ бота на запрос пользователя.\n\n" # TODO: Customize text
        "Этот текст имитирует ответ AI-модели в режиме диалога. "
        "Он должен быть нейтральным и соответствовать логике, заложенной в промпте. "
        "Сгенерированный ответ должен быть основан на предоставленных пользователем данных."
    )
    await message.answer(text)

@router.message(Command("mock_rag"))
async def mock_rag(message: types.Message):
    if message.from_user.id not in ADMIN_IDS: return

    text = (
        "Результат обработки запроса с использованием RAG-данных.\n\n" # TODO: Customize text
        "Этот текст демонстрирует, как AI-модель использует исторические данные "
        "пользователя для формирования ответа.\n\n"
        "1. **Пример данных 1:** Использование контекста.\n"
        "2. **Пример данных 2:** Использование контекста.\n\n"
        "Сгенерированный ответ."
    )
    await message.answer(text, parse_mode="Markdown")
